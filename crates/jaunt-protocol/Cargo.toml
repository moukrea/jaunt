[package]
name = "jaunt-protocol"
version = "0.3.0"
edition = "2021"
description = "Shared protocol types for Jaunt"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rmp-serde = "1"
base64 = "0.22"
